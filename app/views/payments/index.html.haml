%p#notice= notice
%h1 Course Payments
%table
  %thead
    %tr
      %th Course
      %th User
      %th Amount
      %th Stripe charge
      %th{:colspan => "3"}
  %tbody
    - @payments.each do |payment|
      %tr
        %td= payment.course_id
        %td= payment.user_id
        %td= payment.amount
        %td= payment.stripe_charge_id
        %td= link_to 'Show', payment
        %td= link_to 'Edit', edit_payment_path(payment)
        %td= link_to 'Destroy', payment, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Course Payment', new_payment_path
